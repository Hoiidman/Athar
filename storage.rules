rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default deny. Real rules mirror firestore.rules visibility model
    // once memories/{memoryId} storage paths exist (see Database_Schema.md).
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
